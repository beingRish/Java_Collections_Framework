double-ended queue
allows insertion and removal of elements from both ends
versatile than regular queues and stacks because they support the operations of both

INSERTION METHODS

addFirst(E e): Inserts the specified element at the front.
addLast(E e): Inserts the specified element at the end.
offerFirst(E e): Inserts thee specifieed element at the front if possible.
offerLast(E e): Inserts the specifieed element at the end if possible

REMOVAL METHODS

removeFirst(E e): Retrieves and removes the first element.
removeLast(E e): Retrieves and removes the last element.
pollFirst(E e): Retrieves and removes the first element, or returns null if empty.
pollLast(E e): Retrieves and removes the last element, or returns null if empty.


EXAMINATION METHODS

getFirst(E e): Retrieves, but does not remove the first element.
getLast(E e): Retrieves, but does not remove the last element.
peekFirst(E e): Retrieves, but does not remove the first element, or returns null if empty.
peekLast(E e): Retrieves, but does not remove the last element, or returns null if empty.

STACK METHODS

push(E e): Adds an element at the front (equivalent to addFirst(E e)).
pop(): Removes and returns the first element (equivalent to removeFirst()).